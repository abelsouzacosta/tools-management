{
  "type": "postgres",
  "host": "localhost",
  "username": "docker",
  "password": "localhost",
  "database": "unid",
  "entities": [
    "./src/modules/**/entities/*.ts"
  ],
  "migrations": ["./src/database/migrations/*.ts"],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
